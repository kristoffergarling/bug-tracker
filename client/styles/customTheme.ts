import { createTheme } from '@mui/material/styles'

const customTheme = createTheme({
    palette: {
        primary: {
            main: '#395B64',
        },
        secondary: {
            main: '#A5C9CA',
        }
    },
});

export default customTheme;
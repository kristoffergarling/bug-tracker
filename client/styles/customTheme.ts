import { createTheme } from "@mui/material/styles";

const customTheme = createTheme({
  palette: {
    primary: {
      main: "#395B64",
    },
    secondary: {
      main: "#A5C9CA",
    },
    background: {
      default: "#E7F6F2",
    },
  },
});

export default customTheme;
